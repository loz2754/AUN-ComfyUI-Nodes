# AUNStrip — AUN Strip

Purpose: Trim leading/trailing digits and spaces from a string.

## Inputs

### Required

- `text` (STRING, input): String to clean.

## Output

- `cleaned text` (STRING): `text` with any leading/trailing `0–9` and spaces removed.

## Notes

- Handy for cleaning up auto-numbered filenames or labels.
