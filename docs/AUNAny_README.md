# AUNAny — AUNAny

Purpose: Pass-through node that accepts and returns *any* type.

## Inputs

### Required

- `any` (Any): Universal input socket.

## Outputs

- `scheduler` (Any): The same value passed through unchanged.

## Notes

- This is mainly a wiring/organization helper (e.g., to route values cleanly across a graph).
